stages:
  - test

test:  # Because the herokuish buildpacks doesnt support Python Testing
  stage: test
  image: python:3.8
  script:
    - echo "Running custom tests instead of Auto DevOps tests"
    - pip install -r requirements.txt
    - pytest  tests/
  only:
    - main
